let time f x =
    let t = Sys.time () in
    let fx = f x in
    Printf.printf " Execution time: %fs\n" ( Sys.time () -. t );
    fx

let rec fib x =
    match x with
    | 0 -> 1
    | 1 -> 1
    | w -> fib (w-2) + fib (w-1);;
